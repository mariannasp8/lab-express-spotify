<h1>Artist tracks</h1>
{{#each artists}}
  <div class="audio-container">
    <div class="artists-name1">
      <p class="audio1">{{this.name}}</p>
      <audio controls src="{{this.preview_url}}">
      </audio>
    </div>
  </div>
{{/each}}